- name: ddosify action
  uses: QAInsights/ddosify-action@1.0.1
  env:
      SUPER_SECRET: ${{ secrets.url }}
  with:
    args: "-t $SUPER_SECRET -n 100000 -d 1000"
    
- name: Upload Results
  uses: actions/upload-artifact@v2
  with:
    name: ddosify-results
    path: ./*.log
